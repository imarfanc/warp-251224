---
name: "1.  Read current state of order-js/opus-order-extractor.js: Verify the current structure and where the extraction logic begins."
overview: Update the extraction logic in `order-js/opus-order-extractor.js` with new improved code while preserving the configuration options and clipboard functionality.
todos:
  - id: update-extraction-logic
    content: Update `orderData` structure and extraction logic in `order-js/opus-order-extractor.js`.
    status: completed
  - id: verify-integration
    content: Ensure configuration flags and clipboard logic are preserved and correctly integrated.
    status: completed
isProject: false
---

1.  **Read current state of `order-js/opus-order-extractor.js`**: Verify the current structure and where the extraction logic begins.
2.  **Replace `orderData` initialization**: Use the new structure including `orderUrl`, `transactionId`, and `shippingAddress.cityStateZip`.
3.  **Replace extraction logic**:

    -   Update order number extraction to use `URLSearchParams`.
    -   Update order date extraction with new substring logic.
    -   Update shipping address extraction to include `cityStateZip` and `country` on separate lines.
    -   Update payment method extraction logic.
    -   Update order summary extraction logic.
    -   Update "Placed by" extraction logic.
    -   Update items extraction logic (image URL handling and price/seller extraction from container).

4.  **Preserve existing features**:

    -   Keep the `async` function wrapper.
    -   Keep the `saveToFile` and `enableClipboard` constants.
    -   Keep the `copyToClipboard` utility function.
    -   Keep the conditional logic for console logging, file saving, and clipboard copying at the end.

5.  **Verification**: Ensure no syntax errors are introduced and the script correctly integrates the new logic with the existing configuration and clipboard features.