# 2

## opencode

### Visual check for last step

```md
nvm
node
npm
cargo
rust
bun 
deno
uv

opencode
gemini-cli
claude
codex
kilo

brew
bat

check which & version for all and put it in a nice table format
```

### Apps

```md
go
miniconda
python@3.14
rich
zsh-autocomplete
zsh-autosuggestions
zsh-syntax-highlighting

check if all formulas & casks are already installed & if not install using brew and add to path to make them work properly then check which & version for all & put it in a nice table format for visual verification use rich if available to create table

also install val-town cli `deno install -grAf jsr:@valtown/vt` & add to PATH
```

### Casks

```sh
brew install --cask font-hack-nerd-font
brew install --cask handbrake-app
brew install --cask ghostty
brew install --cask obsidian

i installed all the casks above using brew and now i want to check which & version for all & put it in a nice table format for visual verification use rich if available to create table
```

### Formulas

```sh
brew install bat
brew install biome
brew install fd
brew install ffmpeg
brew install fzf
brew install gum
brew install handbrake
brew install imagemagick
brew install ghostscript
brew install just
brew install jq
brew install lame
brew install lsd
brew install markdownlint-cli2
brew install obsidian-cli
brew install steipete/tap/peekaboo
brew install ripgrep
brew install sqlite
brew install tree
brew install xz
brew install yazi
brew install yt-dlp
brew install zoxide
brew install zstd

i installed all the formulas above using brew and now i want to check which & version for all & put it in a nice table format for visual verification use rich if available to create table
```
