# 3

## vt cli

[https://github.com/val-town/vt](https://github.com/val-town/vt)

```bash
#deno install -grAf jsr:@valtown/vt
vt --version
```

## opencode

### more Casks

#### 1

```sh
brew install --cask google-chrome            # imarfanc
brew install --cask google-chrome@beta       # highfillmart
brew install --cask firefox@beta              # aazad.hoon2
brew install --cask firefox@developer-edition # tosifac
brew install --cask brave-browser            # 
brew install --cask brave-browser@beta       # 

i installed all the casks above using brew and now i want to check which & version for all & put it in a nice table format for visual verification use rich if available to create table
```

#### 2

```sh
brew install --cask codex-app
brew install --cask opencode-desktop
brew install --cask keyboard-maestro
brew install --cask whatsapp
brew install --cask shortcat
brew install --cask voiceink
brew install --cask setapp
brew install --cask chatgpt
brew install --cask claude
brew install --cask ghostty
brew install --cask hyperkey
brew install --cask hammerspoon
brew install --cask lunar
brew install --cask zen
brew install --cask github@beta
brew install --cask t3-code
brew install --cask visual-studio-code
brew install --cask antigravity
brew install --cask cursor

i installed all the casks above using brew and now i want to check which & version for all & put it in a nice table format for visual verification use rich if available to create table
```

#### 3

```sh
brew install --cask arc
brew install --cask thebrowsercompany-dia
brew install --cask comet
brew install --cask chatgpt-atlas
brew install --cask helium-browser
#brew install --cask google-chrome@dev       # chrome dev       # 
#brew install --cask google-chrome@canary    # chrome canary    # 
brew install --cask firefox@beta
brew install --cask firefox@developer-edition
brew install --cask brave-browser@nightly

i installed all the casks above using brew and now i want to check which & version for all & put it in a nice table format for visual verification use rich if available to create table
```
