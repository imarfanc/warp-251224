# warp (even more tools/Apps)

## more cli tools

```sh
brew install fd
brew install ffmpeg
brew install handbrake
brew install imagemagick
brew install ghostscript
brew install just
brew install jq
brew install lame
brew install sqlite
brew install xz
brew install yazi
brew install yt-dlp
brew install zoxide
brew install zstd

# maybe
brew install rich-cli
brew install gum
brew install biome
brew install markdownlint-cli2
brew install obsidian-cli
brew install steipete/tap/peekaboo
```

## more casks (browsers)

```sh
brew install --cask google-chrome               # imarfanc
brew install --cask google-chrome@beta          # highfillmart + dba.sh_oil
# brew install --cask brave-browser               # tosifac
# brew install --cask brave-browser@beta          # cgshaq
# brew install --cask firefox@beta                # cgshaq
brew install --cask firefox@developer-edition   # aazad.hoon2
```

```sh
brew install --cask google-chrome                 # aazad
# brew install --cask google-chrome@beta            # rebecca
# brew install --cask brave-browser                 # jennifer
# brew install --cask brave-browser@beta            # 
# brew install --cask firefox@beta                  # kim
brew install --cask firefox@developer-edition     # 
```