# Warp Launch Configuration
#
#
# Use this to start a certain configuration of windows, tabs, and panes.
# Open the launch configuration palette to access and open any launch configuration.
#
# This file defines your launch configuration.
# More on how to do so here:
# https://docs.warp.dev/terminal/sessions/launch-configurations
#
# All launch configurations are stored under ~/.warp/launch_configurations.
# Edit them anytime!
#
# You can also add commands that run on-start for your launch configurations like so:
# ---
# name: Example with Command
# windows:
#  - tabs:
#      - layout:
#          cwd: /Users/warp-user/project
#          commands:
#            - exec: code .

---
name: test1
active_window_index: 0
windows:
  - active_tab_index: 2
    tabs:
      - layout:
          cwd: /Users/family
          is_focused: true
          pane_mode: terminal
      - layout:
          cwd: /Users/family/Developer/gh
          is_focused: true
          pane_mode: terminal
      - layout:
          cwd: /Users/family/Developer/gh/vt
          is_focused: true
          pane_mode: terminal
